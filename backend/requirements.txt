fastapi>=0.110.0
uvicorn[standard]==0.22.0
redis==5.0.0
pydantic>=2.0
sqlalchemy==2.0.19
psycopg2-binary==2.9.7
minio==7.1.3
boto3==1.28.57
requests==2.31.0
pandas==2.2.3
numpy==1.26.4
yfinance==0.2.24
python-dotenv==1.0.0
Pillow==10.0.1
python-multipart==0.0.6
matplotlib>=3.7.0
# TA-Lib is provided in the Docker image via conda (conda-forge).
# Removed from pip requirements to avoid building from source inside the image.
# Optional LLM packages removed for local build compatibility.
# To enable LLM features, add compatible versions and set up credentials.